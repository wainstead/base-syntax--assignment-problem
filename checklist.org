1. [X] Create TWO new components: UserInput and UserOutput
2. [X] UserInput should hold an input element, UserOutput two paragraphs
3. [X] Output multiple UserOutput components in the App component (any
   paragraph texts of your choice)
4. [X] Pass a username (of your choice) to UserOutput via props and
   display it there
5. [X] Add state to the App component (=> the username) and pass the
   username to the UserOutput component
6. [X] Add a method to manipulate the state (=> an event-handler
   method)
7. [X] Pass the event-handler method reference to the UserInput
   component and bind it to the input-change event
8. [X] Ensure that the new input entered by the user overwrites the
   old username passed to UserOutput
9. [X] Add two-way-binding to your input (in UserInput) to also
   display the starting username
10. [X] Add styling of your choice to your components/ elements in the
    components - both with inline styles and stylesheets
